[package]
name = "pillow-rs-image"
version = "0.1.0"
edition = "2021"
description = "Zero-dependency pixel-perfect image decoders (JPEG, PNG, GIF, BMP, WebP, TIFF)"

[dependencies]
# zero dependencies — pure Rust from scratch

[dev-dependencies]
