- id: ftcolor.FT_COLR_COMPOSITE_EXCLUSION
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_EXCLUSION
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 22 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as EXCLUSION for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_EXCLUSION plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_EXCLUSION/22, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_HARD_LIGHT
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_HARD_LIGHT
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 19 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as HARD_LIGHT for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_HARD_LIGHT plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_HARD_LIGHT/19, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_HSL_COLOR
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_HSL_COLOR
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 26 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as HSL_COLOR for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_HSL_COLOR plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_HSL_COLOR/26, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_HSL_HUE
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_HSL_HUE
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 24 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as HSL_HUE for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_HSL_HUE plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_HSL_HUE/24, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_HSL_LUMINOSITY
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_HSL_LUMINOSITY
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 27 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as HSL_LUMINOSITY for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_HSL_LUMINOSITY plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_HSL_LUMINOSITY/27, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_HSL_SATURATION
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_HSL_SATURATION
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 25 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as HSL_SATURATION for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_HSL_SATURATION plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_HSL_SATURATION/25, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_LIGHTEN
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_LIGHTEN
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 16 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as LIGHTEN for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_LIGHTEN plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_LIGHTEN/16, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_MAX
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_MAX
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum sentinel from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 28 matches the pinned C public header and generated FreeType reference docs.
    - id: sentinel_not_emitted_by_valid_paint_graph
      description: Valid COLR v1 PaintComposite traversal never returns FT_COLR_COMPOSITE_MAX as a real composite operation.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with valid PaintComposite nodes covering real modes plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON for every valid composite node and no node with composite_mode=FT_COLR_COMPOSITE_MAX/28; comparison rule=exact C oracle absence/presence classification and exact graph shape.

- id: ftcolor.FT_COLR_COMPOSITE_MULTIPLY
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_MULTIPLY
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 23 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as MULTIPLY for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_MULTIPLY plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_MULTIPLY/23, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_OVERLAY
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_OVERLAY
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 14 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as OVERLAY for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_OVERLAY plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_OVERLAY/14, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_PLUS
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_PLUS
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 12 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as PLUS for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_PLUS plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_PLUS/12, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_SCREEN
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_SCREEN
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 13 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as SCREEN for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_SCREEN plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_SCREEN/13, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_SOFT_LIGHT
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_SOFT_LIGHT
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 20 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as SOFT_LIGHT for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_SOFT_LIGHT plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_SOFT_LIGHT/20, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_SRC
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_SRC
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 1 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as SRC for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_SRC plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_SRC/1, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_SRC_ATOP
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_SRC_ATOP
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 9 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as SRC_ATOP for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_SRC_ATOP plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_SRC_ATOP/9, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_SRC_IN
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_SRC_IN
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 5 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as SRC_IN for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_SRC_IN plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_SRC_IN/5, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_SRC_OUT
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_SRC_OUT
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 7 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as SRC_OUT for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_SRC_OUT plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_SRC_OUT/7, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_SRC_OVER
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_SRC_OVER
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 3 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as SRC_OVER for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_SRC_OVER plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_SRC_OVER/3, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_COMPOSITE_XOR
  kind: enum_variant
  symbol: FT_COLR_COMPOSITE_XOR
  header: freetype/ftcolor.h
  description: FreeType COLR v1 composite-mode enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 11 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_composite_mode_runtime
      description: FT_Get_Paint exposes FT_PaintComposite::composite_mode as XOR for a COLR v1 PaintComposite node using that mode.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintComposite node using FT_COLR_COMPOSITE_XOR plus a COLR v0 control font; params glyph_id=<composite base glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COMPOSITE, composite_mode=FT_COLR_COMPOSITE_XOR/11, source/backdrop opaque paint references, and terminal paint nodes; comparison rule=exact C oracle enum value, graph shape, glyph IDs, palette indexes, and opaque-paint traversal order.

- id: ftcolor.FT_COLR_PAINTFORMAT_COLR_GLYPH
  kind: enum_variant
  symbol: FT_COLR_PAINTFORMAT_COLR_GLYPH
  header: freetype/ftcolor.h
  description: FreeType COLR v1 paint-format enum variant from freetype/ftcolor.h.
  cases:
    - id: value_matches_header
      description: Numeric value 11 matches the pinned C public header and generated FreeType reference docs.
    - id: paint_colr_glyph_runtime
      description: FT_Get_Paint exposes FT_COLR_PAINTFORMAT_COLR_GLYPH and FT_PaintColrGlyph::glyphID for a COLR v1 PaintColrGlyph node.
  # needs-input: operation=FT_Get_Color_Glyph_Paint followed by FT_Get_Paint traversal; fixture_asset=COLR v1 font with a PaintColrGlyph node referencing another BaseGlyphV1List glyph plus a COLR v0 control font; params glyph_id=<base glyph containing PaintColrGlyph>, referenced_glyph_id=<BaseGlyphV1List glyph>, palette_index=0 and nonzero palette if available, root_transform=FT_COLOR_NO_ROOT_TRANSFORM; expected output shape=paint graph JSON containing format FT_COLR_PAINTFORMAT_COLR_GLYPH/11 and colr_glyph.glyphID, with recursive referenced glyph root paint available from FT_Get_Color_Glyph_Paint; comparison rule=exact C oracle enum value, referenced glyph ID, graph shape, palette indexes, and recursion/root-transform behavior.
